[minphony]
# This project uses its own target conventions rather than the GNU defaults
# (all, clean, test). Disable the minimum-phony requirement.
required_phony_targets=

[maxbodylength]
# Recipes in this project intentionally encode multi-step orchestration.
# Allow up to 35 lines per target body.
body_length=35
